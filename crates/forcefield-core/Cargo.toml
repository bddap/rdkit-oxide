[package]
name = "forcefield-core"
version = "0.1.0"
edition = "2021"

[dependencies]
rdkit-core = { path = "../rdkit-core" }
forcefield-uff = { path = "../forcefield-uff" }
serde = { version = "1", features = ["derive"] }
thiserror = "1"

[dev-dependencies]
approx = "0.5"

[lib]
path = "src/lib.rs"