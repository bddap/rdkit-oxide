
[package]
name = "forcefield-uff"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
once_cell = "1.19"

phf = { version = "0.11", features = ["macros"] }

[build-dependencies]
phf_codegen = "0.11"
regex = "1"

[lib]
path = "src/lib.rs"
